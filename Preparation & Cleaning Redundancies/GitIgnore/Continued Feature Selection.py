import pandas as pd

df = pd.read_csv()

rename variables to 'Residual operational categories'
# The data is grouped by consituency group.  With con being the % of adult % when grouped.

# Create ID and group by whatever you feel appropriate to join .

# I want figures for each socio-economic group by post code
